diff --git a/src/locker.rs b/src/locker.rs
index 49b13fe..643dd4e 100644
--- a/src/locker.rs
+++ b/src/locker.rs
@@ -285,6 +283,7 @@ pub enum Message {
     Lock,
     Unlock,
     SpinnerTick,
+    LogOut,
 }
 
 impl From<common::Message> for Message {
@@ -450,6 +449,13 @@ impl App {
                     widget::text(fl!("suspend")),
                     widget::tooltip::Position::Top
                 ),
+                widget::tooltip(
+                    widget::button::custom(widget::icon::from_name("system-log-out-symbolic"))
+                        .padding(12.0)
+                        .on_press(Message::LogOut),
+                    widget::text("Log Out"),
+                    widget::tooltip::Position::Top
+                ),
             ]
             .padding([16.0, 0.0, 0.0, 0.0])
             .spacing(8.0);
@@ -1087,6 +1093,15 @@ impl cosmic::Application for App {
                         cosmic::task::message(cosmic::Action::App(Message::Error(err.to_string())))
                     });
             }
+            Message::LogOut => {
+                #[cfg(feature = "logind")]
+                return cosmic::Task::future(async move { crate::logind::terminate_session().await.err() })
+                    .and_then(|err| {
+                        tracing::error!("failed to logout: {:?}", err);
+                        cosmic::task::message(cosmic::Action::App(Message::Error(err.to_string())))
+                    });
+
+            }
             Message::TimeAppletConfig(config) => {
                 self.flags.user_data.time_applet_config = config;
             }
diff --git a/src/logind.rs b/src/logind.rs
index d4f216c..42dd158 100644
--- a/src/logind.rs
+++ b/src/logind.rs
@@ -3,7 +3,7 @@ use cosmic::iced::{
     futures::{SinkExt, StreamExt, channel::mpsc},
 };
 use logind_zbus::{
-    manager::{InhibitType, ManagerProxy},
+    manager::{InhibitType, ManagerProxy, SessionInfo},
     session::SessionProxy,
 };
 use std::{any::TypeId, error::Error, os::fd::OwnedFd, sync::Arc, time::Duration};
@@ -29,6 +29,19 @@ pub async fn suspend() -> zbus::Result<()> {
     manager.suspend(false).await
 }
 
+pub async fn list_sessions() -> zbus::Result<Vec<SessionInfo>> {
+    let connection = Connection::system().await?;
+    let manager = ManagerProxy::new(&connection).await?;
+    manager.list_sessions().await
+}
+
+pub async fn terminate_session() -> zbus::Result<()> {
+    let connection = Connection::system().await?;
+    let manager = ManagerProxy::new(&connection).await?;
+    let user = (list_sessions().await).unwrap()[0].sid();
+    manager.terminate_session(user).await
+}
+
 async fn inhibit(manager: &ManagerProxy<'_>) -> zbus::Result<OwnedFd> {
     let what = InhibitType::Sleep;
     let who = "COSMIC Greeter";

